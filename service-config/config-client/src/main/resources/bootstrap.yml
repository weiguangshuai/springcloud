spring:
  cloud:
    config:
      profile: test
      label: master
      uri: http://localhost:1116
  application:
    name: config-client

server:
  port: 1117
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/